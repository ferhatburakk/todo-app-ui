export interface TodoType {
    id: Number;
    content: string;
    header: string;
    isCompleted: Number;
};

export interface UserType {
    id: number;
    userName: string;
    password: string;
}